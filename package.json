{
  "devDependencies": {
    "netlify-plugin-submit-sitemap": "^0.4.0"
"scripts": {
  "start": "react-scripts start",
  "build": "react-scripts build && cp robots.txt sitemap.xml build/",
  "test": "react-scripts test",
  "eject": "react-scripts eject"
}
